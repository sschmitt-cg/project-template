# CLAUDE.md

Shared rules (behavior, git, code style, standard files) are inherited from `~/Documents/code-projects/CLAUDE.md`. The Scottsidian vault lookup is handled by the global `~/.claude/CLAUDE.md`.

Add only project-specific overrides below.

---

<!-- Add project-specific overrides below this line if needed -->
